# Shopro AI 厂商运营后台功能说明文档

## 1. 文档概述

### 1.1 项目名称

Shopro AI 厂商运营后台 Demo。

### 1.2 项目定位

本系统面向 Shopro AI 电商 AIGC 平台的运营管理人员，用于集中查看平台经营数据、管理用户与积分、处理 AI 生成任务、审核平台内容、查看订单信息以及监控系统运行状态。

当前项目为前端演示系统，主要用于产品展示、业务流程演示和后续真实后端接入的界面基础。

### 1.3 当前实现边界

- 前端采用 Vue 3 + TypeScript 实现。
- 默认通过 MSW 模拟 `/api/admin/**` 接口。
- 演示数据保存在浏览器 `localStorage` 中。
- 当前不连接真实数据库、支付服务、AI 服务或 Spring Boot 后端。
- 登录、角色和权限目前仅为演示实现，不具备生产级安全能力。

---

## 2. 用户角色与登录

### 2.1 管理员登录

系统提供独立的管理员登录页面，访问后台业务页面前必须完成登录。

**登录入口：** `/login`

**演示账号：** `admin@shopro.ai`

**演示规则：** 密码可任意填写，Mock 登录接口不进行真实密码校验。

### 2.2 登录状态

- 登录成功后保存管理员信息和登录状态。
- 未登录访问后台页面时，自动跳转至登录页。
- 已登录用户访问登录页时，自动跳转至运营总览页。
- 点击退出登录后清除本地登录状态。

### 2.3 权限说明

当前版本仅模拟 `SUPER_ADMIN` 管理员身份，未实现基于角色的菜单、按钮和接口权限控制。生产环境可由 Spring Security 接入 JWT 与 RBAC 权限体系。

---

## 3. 系统整体功能

登录后进入后台管理布局，系统包含以下六个主要功能模块：

| 模块 | 路由 | 功能概要 |
|---|---|---|
| 运营总览 | `/dashboard` | 查看平台核心经营指标、任务趋势和待审核内容 |
| 用户与积分 | `/users` | 查询用户、管理账号状态和调整积分 |
| AI 任务中心 | `/jobs` | 查看视频、脚本、配音任务并处理失败或进行中的任务 |
| 内容审核 | `/contents` | 审核商品、脚本和作品内容 |
| 订单与套餐 | `/orders` | 查看订单、套餐、金额及支付状态 |
| 系统运营 | `/system` | 查看服务健康状态、操作日志和恢复演示数据 |

后台布局包含深色侧边栏、顶部标题栏、Demo 数据标识、管理员信息、通知入口和退出登录入口。

---

## 4. 功能模块说明

## 4.1 运营总览

**页面：** `/dashboard`

运营总览用于帮助管理员快速掌握平台整体运营情况。

### 功能内容

1. **核心指标展示**
   - 注册用户数
   - 今日营收
   - AI 生成任务数
   - 待审核内容数

2. **任务趋势分析**
   - 展示近 7 日 AI 任务数量趋势。
   - 通过折线图展示每日任务变化情况。

3. **任务状态分布**
   - 成功任务
   - 进行中任务
   - 失败任务
   - 通过环形图展示任务状态比例。

4. **待处理事项**
   - 展示待审核内容列表。
   - 显示内容标题、类型、提交人和审核状态。

5. **数据刷新**
   - 支持手动刷新运营数据。
   - 页面进入时自动加载数据。

### 说明

当前部分营收和趋势数据为固定 Demo 数据，任务数量和待审核数量会根据本地 Mock 数据计算。

---

## 4.2 用户与积分管理

**页面：** `/users`

用户与积分管理用于处理平台用户账号和平台积分相关运营工作。

### 用户查询

- 按姓名或邮箱进行关键词搜索。
- 按账号状态筛选。
- 支持分页查看用户列表。

### 用户信息

列表展示以下字段：

- 用户名
- 邮箱
- 用户类型
- 当前积分
- 账号状态
- 注册时间
- 操作入口

用户类型包括普通用户和企业用户，账号状态包括启用和禁用。

### 账号状态管理

管理员可以启用或禁用用户账号。

操作流程：

1. 点击启用或禁用。
2. 弹出二次确认提示。
3. 调用账号状态更新接口。
4. 更新用户数据。
5. 显示操作结果提示。
6. 刷新用户列表并记录操作日志。

### 积分调整

管理员可以为用户增加或扣减积分。

操作流程：

1. 点击积分调整。
2. 输入积分变动数值，正数表示增加，负数表示扣减。
3. 提交积分调整。
4. 更新用户当前积分。
5. 显示操作结果并记录操作日志。

### 当前限制

当前未实现用户详情、积分流水、调整原因、积分上限校验和细粒度权限控制。

---

## 4.3 AI 任务中心

**页面：** `/jobs`

AI 任务中心用于管理平台中的视频、脚本和配音生成任务。

### 任务类型

- 视频生成
- 脚本生成
- 配音生成

### 任务状态

| 状态 | 说明 |
|---|---|
| 排队中 | 任务等待处理 |
| 生成中 | AI 服务正在处理任务 |
| 成功 | 任务已完成 |
| 失败 | 任务处理失败 |
| 已取消 | 任务被管理员取消 |

### 列表功能

- 按任务状态筛选。
- 支持分页。
- 查看任务名称、类型、状态和进度。
- 失败任务显示错误原因。

### 失败任务重试

失败任务支持查看原因并重新发起处理。

操作流程：

1. 点击查看原因或重试。
2. 查看任务失败原因。
3. 确认重新执行任务。
4. 任务状态变为“生成中”，进度变为 15%。
5. Mock 环境约 3 秒后自动变为“成功”，进度变为 100%。
6. 页面刷新任务列表并记录操作日志。

### 任务取消

排队中或生成中的任务支持取消。

操作流程：

1. 点击取消任务。
2. 进行二次确认。
3. 调用取消接口。
4. 任务状态变为“已取消”。
5. 刷新列表并记录操作日志。

### 当前限制

当前未接入真实 AI 模型、任务队列、媒体文件、实时进度推送、任务详情和 WebSocket/SSE 通知。

---

## 4.4 内容审核

**页面：** `/contents`

内容审核用于管理平台中的商品、脚本和作品内容，控制内容是否可以继续展示或使用。

### 内容类型

- 商品
- 脚本
- 作品

### 审核状态

| 状态 | 说明 |
|---|---|
| 待审核 | 等待运营人员处理 |
| 已通过 | 审核通过，可继续使用 |
| 已驳回 | 审核未通过 |
| 已下架 | 内容不再对外展示 |

### 列表功能

- 按审核状态筛选。
- 支持分页。
- 查看内容标题、内容类型、作者和当前状态。

### 审核操作

1. **通过**：将待审核内容设置为已通过。
2. **驳回**：将待审核内容设置为已驳回。
3. **下架**：将已通过内容设置为已下架。

所有状态变更均需要二次确认，操作成功后刷新列表并写入操作日志。

### 当前限制

当前未实现内容详情、媒体预览、审核意见、敏感词检测、批量审核和独立的内容上下架接口。

---

## 4.5 订单与套餐

**页面：** `/orders`

订单与套餐模块用于查看平台用户的套餐购买记录和订单支付状态。

### 订单列表

列表展示以下字段：

- 订单号
- 用户
- 套餐名称
- 订单金额
- 创建时间
- 支付状态

### 套餐类型

- 体验套餐
- 专业套餐
- 企业套餐

### 支付状态

- 已支付
- 待支付
- 退款中
- 已退款

### 查询功能

- 按支付状态筛选。
- 支持分页查看订单。

### 统计卡片

页面展示以下运营统计信息：

- 本月成交额
- 支付订单数
- 专业套餐占比
- 退款率

当前统计数据主要用于演示，部分数据直接由页面展示。

### 当前限制

当前未实现订单详情、套餐配置、真实支付同步、支付回调、退款操作、订单导出和积分流水查询。

---

## 4.6 系统运营

**页面：** `/system`

系统运营模块用于查看基础服务运行状态、后台操作记录，并恢复演示环境。

### 服务健康状态

当前展示以下服务：

- API 网关
- 视频生成服务
- 模型队列

每项服务展示健康状态和响应延迟，状态包括正常和需要关注。

### 操作日志

日志用于记录后台关键管理操作，包括：

- 用户启用或禁用
- 用户积分调整
- AI 任务重试
- AI 任务重试完成
- AI 任务取消
- 内容审核

日志展示操作时间、操作人和操作内容。

### 恢复演示数据

用于将被修改的本地 Demo 数据恢复为初始状态。

操作流程：

1. 点击恢复演示数据。
2. 进行二次确认。
3. 清除浏览器中的 Demo 数据。
4. 重新加载初始种子数据。

该功能适合演示前初始化系统，或在操作流程结束后恢复环境。

---

## 5. 数据与接口说明

### 5.1 当前接口

```text
POST  /api/admin/auth/login
GET   /api/admin/dashboard

GET   /api/admin/users
PATCH /api/admin/users/:id/status
POST  /api/admin/users/:id/credits

GET   /api/admin/jobs
POST  /api/admin/jobs/:id/retry
POST  /api/admin/jobs/:id/cancel

GET   /api/admin/contents
PATCH /api/admin/contents/:id/review

GET   /api/admin/orders

GET   /api/admin/system/health
GET   /api/admin/audit-logs
POST  /api/admin/demo/reset
```

### 5.2 通用响应结构

接口采用统一响应结构：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

分页接口返回：

```json
{
  "items": [],
  "total": 0,
  "page": 1,
  "pageSize": 10
}
```

### 5.3 Demo 数据规模

系统首次运行时生成以下演示数据：

- 用户 120 条
- AI 任务 90 条
- 内容 50 条
- 订单 80 条
- 操作日志 100 条

数据保存在 `localStorage["shopro-admin-demo"]` 中，刷新浏览器后仍然保留。

---

## 6. 典型业务流程

### 6.1 管理员登录流程

```text
访问后台 → 未登录跳转登录页 → 输入账号 → 调用登录接口 → 保存登录状态 → 进入运营总览
```

### 6.2 用户运营流程

```text
进入用户管理 → 搜索或筛选用户 → 查看用户状态
→ 启用/禁用账号或调整积分 → 二次确认 → 更新数据 → 写入操作日志
```

### 6.3 AI 任务处理流程

```text
进入任务中心 → 筛选任务 → 查看失败原因
→ 确认重试 → 生成中 → 模拟完成
```

或：

```text
进入任务中心 → 查看排队中/生成中任务 → 确认取消 → 任务变为已取消
```

### 6.4 内容审核流程

```text
进入内容审核 → 筛选待审核内容 → 通过或驳回
→ 二次确认 → 更新审核状态 → 记录操作日志
```

### 6.5 演示数据恢复流程

```text
系统运营 → 恢复演示数据 → 二次确认 → 清理本地修改 → 恢复初始数据
```

---

## 7. 技术架构与部署

### 7.1 技术栈

- Vue 3
- TypeScript
- Vite
- Vue Router
- Pinia
- Element Plus
- Axios
- ECharts
- MSW
- Day.js
- pnpm

### 7.2 数据请求链路

```text
页面与组件
    ↓
src/api API 封装
    ↓
Axios Client
    ↓
/api/admin/**
    ├── Demo 环境：MSW Mock
    └── 生产环境：Spring Boot REST API
```

### 7.3 环境配置

```env
VITE_USE_MOCK=true
VITE_API_BASE_URL=/api
```

- `VITE_USE_MOCK=true`：启用 MSW 模拟接口。
- `VITE_USE_MOCK=false`：关闭 Mock，改为请求真实后端。
- `VITE_API_BASE_URL`：配置 API 请求基础地址。

### 7.4 启动与构建

```bash
pnpm install
pnpm dev
pnpm build
pnpm preview
```

构建产物位于 `dist/`，可部署到静态托管平台，也可复制至 Spring Boot 的静态资源目录。

---

## 8. 非功能说明与生产化注意事项

当前版本适合产品演示和前端联调，不建议直接用于生产环境。正式上线前需要补充：

- 真实管理员认证、JWT/Session 和登录安全校验。
- 基于角色的菜单、按钮和接口权限控制。
- 服务端数据库及多用户数据隔离。
- 真实 AI 任务队列和任务进度通知。
- 真实支付、退款和订单状态同步。
- 内容详情、媒体预览及审核意见。
- 完整的参数校验、异常处理和接口错误提示。
- 操作日志的服务端持久化和审计查询能力。
- 日期范围筛选、排序、导出和更完整的分页查询。

---

## 9. 总结

Shopro AI 厂商运营后台 Demo 以运营闭环为核心，覆盖“经营数据查看、用户管理、AI 任务处理、内容审核、订单查看、系统监控”六大场景。系统通过 Mock API 和本地持久化数据完整模拟了后台的主要操作流程，为后续接入 Spring Boot、真实数据库、AI 服务、支付系统和权限体系预留了统一的 `/api/admin/**` 接口边界。
